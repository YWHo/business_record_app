export function DocumentsPage() {
  return (
    <section aria-labelledby="documents-heading">
      <div className="page-heading">
        <div>
          <span className="eyebrow">Business records</span>
          <h1 id="documents-heading">Documents</h1>
          <p>
            Supporting documents remain attached to their records while the
            focused document workspace is prepared.
          </p>
        </div>
      </div>
    </section>
  );
}
